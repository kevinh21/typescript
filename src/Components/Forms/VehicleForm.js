import React from "react";

const VehicleForm = () => {
  return <div>Hello from Vehicles</div>;
};

export default VehicleForm;
// Create a form synamically
// const cForm = document.createElement("cForm");
// cForm.setAttribute("method", "post");
// cForm.setAttribute("action", "submit.php");

//this will be a function call from the orderForm page
// const year = document.createElement("input");
// year.setAttribute("text", "year");
// year.setAttribute("name", "year");
// year.setAttribute("placeholer", "Year");

// const make = document.createElement("input");
// make.setAttribute("text", "make");
// make.setAttribute("name", "make");
// make.setAttribute("placeholder", "Make");

// const model = document.createElement("input");
// model.setAttribute("text", "model");
// model.setAttribute("name", "model");
// model.setAttribute("placeholder", "Model");

// const color = document.createElement("input");
// color.setAttribute("text", "color");
// color.setAttribute("name", "color");
// color.setAttribute("placeholder", "Color");
