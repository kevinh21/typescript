import React from "react";

const PayForm = () => {
  return <div></div>;
};

export default PayForm;

// Create a form synamically
// const cForm = document.createElement("cForm");
// cForm.setAttribute("method", "post");
// cForm.setAttribute("action", "submit.php");

// const dep = document.createElement("input");
// dep.setAttribute("text", "dep");
// dep.setAttribute("name", "deposit");
// dep.setAttribute("placeholder", "Deposit");

// const cash = document.createElement("input");
// cash.setAttribute("text", "cash");
// cash.setAttribute("name", "cash");
// cash.setAttribute("placeholder", "Cash");

// const cc = document.createElement("input");
// cc.setAttribute("text", "cc");
// cc.setAttribute("name", "creditCard");
// cc.setAttribute("placeholder", "Credit Card");

// const bal = document.createElement("input");
// bal.setAttribute("text", "bal");
// bal.setAttribute("name", "balance");
// bal.setAttribute("placeholder", "Balance");

// const total = document.createElement("input");
// total.setAttribute("text", "total");
// total.setAttribute("name", "total");
// total.setAttribute("placeholder", "Total");

// const tax = document.createElement("input");
// tax.setAttribute("text", "tax");
// tax.setAttribute("name", "tax");
// tax.setAttribute("placeholder", "Tax");

// const gTotal= document.createElement("input");
// gTotal.setAttribute("text", "gTotal");
// gTotal.setAttribute("name", "grandTotal");
// gTotal.setAttribute("placeholder", "Grand Total");
